# Prompt Evaluator

Example plugin showing model access, plugin storage, notification dedupe, and typed findings.

This is intentionally small. Production evaluators should keep policy rules explicit, configurable, and testable.

